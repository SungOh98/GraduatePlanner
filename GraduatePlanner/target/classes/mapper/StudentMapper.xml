<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.repository.StudentMapper">
    <insert id="create" parameterType="org.example.domain.Student">
        INSERT INTO student
            (id, current_credits, account, password, student_number, name, year, department_id)
        VALUES (#{id}, #{current_credits}, #{account}, #{password}, #{student_number}, #{name}, #{year}, #{department_id})
    </insert>

</mapper>
